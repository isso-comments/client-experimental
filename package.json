{
  "name": "isso-experiment",
  "scripts": {
    "build": "webpack --config-name dev",
    "watch": "webpack --config-name dev --watch",
    "build-prod": "webpack --merge --config-name dev --config-name prod"
  },
  "devDependencies": {
    "webpack": "^5.72.1",
    "webpack-cli": "^4.9.2"
  }
}
